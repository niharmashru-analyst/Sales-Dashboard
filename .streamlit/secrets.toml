# Set your real admin password here, then keep this file OUT of public GitHub repos.
# On Streamlit Community Cloud: put this same content into
# App settings -> Secrets, instead of committing this file.
admin_password = "changeme123"
